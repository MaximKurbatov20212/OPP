MPI_COMM_Split
MPi_Count_get - 
MPi_Count_create   
MPI_dims_create
своими руками количество процессов
MPI_Scatter - распилить матрицу
размер матрицы(левой) делится на размер сетки
транспонировать нельзя
MPI_Type_vector - свой тип данных(с отсупом)
число блоков- число строк
длина блока - длина строки
отсуп - не понял 
ширина блоков одинакова!!!

1)
Как создать сетку:
        cст
    MPI_Cart_create - создает 2d сетку процессов(можно хардкодить)
    MPI_Cart_sub - из двумерной сетки делает два коммуникатора
2) 5-6 для матрицы A
3) 
9) create type(for_C) 
10) (0,0) Recv(&(C[m][n],...,for_C, ...)) <- (i,j)
